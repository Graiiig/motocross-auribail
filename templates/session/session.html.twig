{% extends 'base.html.twig' %}

{% block title %}Vous êtes inscrit !{% endblock %}

{% block body %}

<div class="container-fluid">
	<div class="row justify-content-center mt-5">
		<div class="col-md-8 col-sm-3">
			<div class="jumbotron text-center open shadow-lg p-3 rounded">
				
				<h5>
					Merci {{app.user.firstName}} vous êtes bien inscrit à {{session.title}}
				</h5>
				<p>
					<a class="btn btn-warning btn-large mt-2" href="{{ path('home') }}">Retour à l'acceuil</a>
				</p>

                {# TODO : ajouter icone partage sur les réseaux sociaux et sur le calendrier #}
                
			</div>
		</div>
	</div>
</div>
</div>

{% endblock %}
