{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>Entraînement n°{{ session.id }} : {{session.title}} le
        {{session.date | format_datetime(locale='fr',pattern="EEEE dd MMMM YYYY") }}</h1>
    <table class="table table-striped table-dark">
        <h1>Liste des utilisateurs :</h1>
        <thead>
            <tr>
                <th scope="col">n°</th>
                <th scope="col">Prénom</th>
                <th scope="col">Nom</th>
                <th scope="col">Date de naissance</th>
                <th scope="col">Téléphone</th>
                <th scope="col">Licence</th>
                <th scope="col">Adresse</th>
                <th scope="col">Supprimer</th>
            </tr>
        </thead>
        <tbody>

        <pre>
        </pre>
            {% for pendingList in pendingLists %}
            <tr>
                <td>{{loop.index}}</td>
                <td>{{ pendingList.user.firstName }}</td>
                <td>{{ pendingList.user.lastName }}</td>
                <td>{{ pendingList.user.birthdate | date('d/m/y') }}</td>
                <td>{{ pendingList.user.phoneNumber }}</td>
                <td>{{ pendingList.user.license }}</td>
                <td>{{ pendingList.user.address }}</td>
                <td>
                    <form method="post" action="{{ path('admin_session_user_delete', {'pendinglist' : pendingList.id, 'sessionId' : session.id}) }}"
                        onsubmit="return confirm('Êtes-vous vraiment sûr ?')">
                        <input type="hidden" name="_method" value="DELETE">
                        <button class="close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </form>
                </td>
            </tr>
            {% endfor %}

        </tbody>
    </table>
</div>

{% endblock %}