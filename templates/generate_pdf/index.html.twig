{# {% extends 'base.html.twig' %} #}

<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" integrity="sha512-iBBXm8fW90+nuLcSKlbmrPcLa0OT92xO1BIsZ+ywDWZCvqsWgccV3gFoRBv0z+8dLJgyAHIhR35VZc2oM/gI1w==" crossorigin="anonymous"/>
</head>
<h3>
  Session :
  {{currentSession.title}}
  le
  {{currentSession.date | format_datetime(locale='fr',pattern="EEEE dd MMMM YYYY")}}</h3>

<body>
<div class="container">
    <div class="row">
    
    
  <table class="table w-50">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Nom</th>
        <th scope="col">Prénom</th>
        <th scope="col">N° Licence</th>
        <th scope="col">Signer ici</th>
      </tr>
    </thead>
    <tbody>

      {% for session in pendingList.adults|slice(0, 33) %}
        
          <tr>
            <td>{{loop.index}}</td>
            <td>{{session.user.email}}</td>
            <td>{{session.user.lastName}}</td>
            <td>{{session.user.firstName}}</td>
            <td></td>
          </tr>
        
      {% endfor %}
    </tbody>
  </table>
    <table class="table w-50">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Nom</th>
        <th scope="col">Prénom</th>
        <th scope="col">N° Licence</th>
        <th scope="col">Signer ici</th>
      </tr>
    </thead>
    <tbody>

      {% for session in pendingList.adults|slice(33, 75) %}
        
          <tr>
            <td>{{loop.index}}</td>
            <td>{{session.user.email}}</td>
            <td>{{session.user.lastName}}</td>
            <td>{{session.user.firstName}}</td>
            <td></td>
          </tr>
        
      {% endfor %}
    </tbody>
  </table>

  </div>
</div>
  <table class="table w-50">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">enfants</th>
        <th scope="col">Prénom</th>
        <th scope="col">N° Licence</th>
        <th scope="col">Signer ici</th>
      </tr>
    </thead>
    <tbody>

      {% for session in pendingList.kids %}
        
          <tr>
            <td>{{loop.index}}</td>
            <td>{{session.user.email}}</td>
            <td>{{session.user.lastName}}</td>
            <td>{{session.user.firstName}}</td>
            <td></td>
          </tr>
        {% endfor %}
      
    </tbody>
  </table>

</body>
